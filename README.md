# Quick QR Premium

Premium QR Scanner + Generator built with React, TypeScript and Vite.

Run: `npm install` then `npm run dev`.

Build: `npm run build`.

Includes premium landing page, camera scanner, QR generator, QR types, customization UI, PNG download, local history and responsive design. No hemasai folder.
